---
phase: 03-semantic-cache-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AIShellCore/Cache/ResponseCache.swift
autonomous: true

must_haves:
  truths:
    - "Semantically similar queries return same cached response"
    - "Exact-match queries still return cached response (backward compatible)"
    - "Cache keys containing pipe characters do not collide"
    - "Similarity threshold is configurable (default 0.92)"
  artifacts:
    - path: "Sources/AIShellCore/Cache/ResponseCache.swift"
      provides: "Semantic cache matching with embedding storage"
      contains: "func findSimilarMatch"
      exports: ["ResponseCache", "CachedResponse"]
  key_links:
    - from: "ResponseCache.get()"
      to: "findSimilarMatch()"
      via: "fallback on exact-match miss"
      pattern: "await findSimilarMatch"
    - from: "ResponseCache.set()"
      to: "ollamaClient.generateEmbedding()"
      via: "embedding generation before storage"
      pattern: "await.*generateEmbedding"
    - from: "findSimilarMatch()"
      to: "cosineSimilarity()"
      via: "similarity calculation"
      pattern: "cosineSimilarity\\("
---

<objective>
Implement semantic cache matching in ResponseCache to return cached responses for queries that are semantically similar (not just identical) to previously cached queries.

Purpose: Increase cache hit rate by matching "list files" and "show files" to the same cached response, reducing redundant Ollama API calls.

Output: Modified ResponseCache.swift with embedding storage, two-level lookup (exact then semantic), and collision-resistant JSON-based cache keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-semantic-cache-matching/03-RESEARCH.md

# Source files
@Sources/AIShellCore/Cache/ResponseCache.swift
@Sources/AIShellCore/RAG/EmbeddingStore.swift
@Sources/AIShellCore/Clients/OllamaClient.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CachedResponse model and fix cache key generation</name>
  <files>Sources/AIShellCore/Cache/ResponseCache.swift</files>
  <action>
1. Add new fields to CachedResponse struct:
   - `embedding: [Double]?` - Query embedding for semantic matching (optional for backward compatibility)
   - `originalQuery: String?` - Original query text for debugging (optional)

2. Update CachedResponse initializer to accept optional embedding and originalQuery parameters with nil defaults.

3. Replace pipe-delimited key generation in `createKey()` with JSON serialization:
   - Define private `CacheKeyComponents` struct inside the method (or as nested type): `{ type: String, content: String, context: [String: String] }`
   - Use JSONEncoder with `.sortedKeys` for deterministic output
   - Hash the JSON bytes with SHA256 (existing pattern)
   - Keep fallback to old method if JSON encoding fails (defensive)

This task addresses R3.1 (embedding storage) and R3.4 (collision-resistant keys).
  </action>
  <verify>
Build succeeds: `swift build 2>&1 | tail -5`

Verify JSON key generation differs for collision case:
- Key for `("cmd", "git commit -m 'x|y'", [:])` should differ from
- Key for `("cmd|git", "commit -m 'x", ["y": ""])`
  </verify>
  <done>
CachedResponse has optional embedding and originalQuery fields. createKey() uses JSON serialization with SHA256 hashing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add semantic lookup to ResponseCache</name>
  <files>Sources/AIShellCore/Cache/ResponseCache.swift</files>
  <action>
1. Add configuration properties to ResponseCache:
   - `private let semanticThreshold: Double` (default 0.92 per R3.3)
   - `private let ollamaClient: OllamaClient?` (optional, nil disables semantic matching)
   - `private let enableSemanticCache: Bool` (feature flag, default true)

2. Update `init()` to accept new parameters:
   ```swift
   public init(
       storageURL: URL?,
       enablePersistence: Bool = true,
       semanticThreshold: Double = 0.92,
       ollamaClient: OllamaClient? = nil,
       enableSemanticCache: Bool = true
   )
   ```

3. Change `get(_ key: String)` to async with optional query parameter:
   ```swift
   public func get(_ key: String, query: String? = nil) async -> String?
   ```
   - Level 1: Exact match lookup (existing behavior, fast path)
   - Level 2: If exact miss AND query provided AND semanticCache enabled, call `findSimilarMatch(query:)`

4. Add private `findSimilarMatch(query: String) async -> String?`:
   - Generate embedding via `ollamaClient.generateEmbedding(text: query)`
   - Scan all non-expired cache entries with embeddings
   - Calculate cosine similarity for each
   - Return response of best match if similarity >= semanticThreshold
   - Log semantic hit with similarity score

5. Add private `cosineSimilarity(_ a: [Double], _ b: [Double]) -> Double`:
   - Copy implementation from EmbeddingStore (lines 446-456)
   - Handle edge cases: different dimensions return 0.0, zero magnitude returns 0.0

6. Add private `isExpired(_ entry: CachedResponse) -> Bool` helper.

This task addresses R3.2 (similarity search) and R3.3 (threshold matching).
  </action>
  <verify>
Build succeeds: `swift build 2>&1 | tail -5`

Code inspection: Verify `get()` is now async and has two-level lookup structure.
  </verify>
  <done>
ResponseCache.get() performs exact match first, then semantic search on miss. Similarity threshold is configurable (default 0.92).
  </done>
</task>

<task type="auto">
  <name>Task 3: Make set() async with embedding generation</name>
  <files>Sources/AIShellCore/Cache/ResponseCache.swift</files>
  <action>
1. Change `set()` signature to async with optional query parameter:
   ```swift
   public func set(
       _ key: String,
       response: String,
       query: String? = nil,
       metadata: [String: String] = [:]
   ) async
   ```

2. In set() implementation:
   - If semanticCache enabled AND query provided AND ollamaClient available:
     - Generate embedding via `try? await ollamaClient.generateEmbedding(text: query)`
   - Else: embedding = nil
   - Create CachedResponse with embedding and originalQuery
   - Store in cache (existing logic)
   - Prune if needed (existing logic)

3. Update callers awareness: The change from sync to async is a breaking change. Document that callers must now `await cache.set()`.

4. Run tests and fix any compilation errors from the async signature change:
   - Tests may need to add `await` to set() calls
   - If tests use mocks, ensure mock conforms to async signature

This task completes R3.1 by storing embeddings when responses are cached.
  </action>
  <verify>
Build succeeds: `swift build 2>&1 | tail -5`
Tests pass: `swift test 2>&1 | tail -10`
  </verify>
  <done>
ResponseCache.set() is async and generates embedding when query is provided. Cache entries contain embeddings for future semantic matching.
  </done>
</task>

</tasks>

<verification>
1. **Build verification:**
   ```bash
   swift build
   ```
   Must compile without errors.

2. **Test verification:**
   ```bash
   swift test
   ```
   All existing tests must pass (may need async updates).

3. **Code inspection checklist:**
   - [ ] CachedResponse has `embedding: [Double]?` and `originalQuery: String?`
   - [ ] createKey() uses JSONEncoder with .sortedKeys
   - [ ] get() is async and calls findSimilarMatch on exact miss
   - [ ] findSimilarMatch() uses cosineSimilarity() and respects semanticThreshold
   - [ ] set() is async and generates embedding when query provided
   - [ ] init() accepts semanticThreshold and ollamaClient parameters
</verification>

<success_criteria>
- Build succeeds with no errors
- All tests pass (61 tests)
- CachedResponse struct has embedding and originalQuery optional fields
- createKey() produces different hashes for the pipe-collision test case
- get() signature is `async` with optional query parameter
- set() signature is `async` with optional query parameter
- semanticThreshold defaults to 0.92 and is configurable via init()
- Semantic matching only triggers when exact match fails AND query is provided
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-cache-matching/03-01-SUMMARY.md`
</output>
