---
phase: 05-m1-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AIShellCore/Server/EnhancedRequestHandler.swift
  - .planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md
  - .planning/phases/02-incremental-pruning/02-01-VERIFICATION.md
autonomous: false

must_haves:
  truths:
    - "Health endpoint returns embedding cache statistics (entries, hits, misses, hit ratio)"
    - "Phase 1 VERIFICATION.md verifies LRU cache implementation truths"
    - "Phase 2 VERIFICATION.md verifies heap-based pruning truths"
    - "Human confirms semantic matching works with similar queries"
  artifacts:
    - path: "Sources/AIShellCore/Server/EnhancedRequestHandler.swift"
      provides: "Health endpoint with embedding cache stats"
      contains: "getCacheStatistics"
    - path: ".planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md"
      provides: "Phase 1 verification"
      min_lines: 50
    - path: ".planning/phases/02-incremental-pruning/02-01-VERIFICATION.md"
      provides: "Phase 2 verification"
      min_lines: 50
  key_links:
    - from: "EnhancedRequestHandler.handleHealth()"
      to: "embeddingStore.getCacheStatistics()"
      via: "await call in health handler"
      pattern: "getCacheStatistics"
---

<objective>
Wire orphaned EmbeddingStore cache statistics to the health endpoint for end-to-end observability, and create verification documents for Phase 1 (Query Embedding Cache) and Phase 2 (Incremental Pruning) to close documentation gaps identified in the M1 audit.

Purpose: Complete the M1 milestone by closing tech debt identified in the audit - ensuring all observability exports are wired and all phases have verification documentation.

Output: Modified health endpoint with embedding cache stats, two VERIFICATION.md files, and human-verified semantic matching.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-query-embedding-cache/01-01-SUMMARY.md
@.planning/phases/02-incremental-pruning/02-01-SUMMARY.md
@.planning/phases/03-semantic-cache-matching/03-VERIFICATION.md
@.planning/phases/04-statistics-optimization/04-01-VERIFICATION.md
@Sources/AIShellCore/Server/EnhancedRequestHandler.swift
@Sources/AIShellCore/RAG/EmbeddingStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire EmbeddingStore cache statistics to health endpoint</name>
  <files>Sources/AIShellCore/Server/EnhancedRequestHandler.swift</files>
  <action>
In `handleHealth()` method (around line 386-391), extend the existing RAG statistics block to also include embedding cache statistics.

After the existing RAG document count logic:
```swift
if enableRAG, let embeddingStore = embeddingStore {
    let stats = await embeddingStore.getStatistics()
    if let docCount = stats["total_documents"] as? Int {
        healthInfo["rag_documents"] = String(docCount)
    }
}
```

Add embedding cache stats by calling `getCacheStatistics()`:
```swift
// Add embedding cache stats (same block)
let cacheStats = await embeddingStore.getCacheStatistics()
healthInfo["embedding_cache_entries"] = String(cacheStats.totalEntries)
healthInfo["embedding_cache_hits"] = String(cacheStats.cacheHits)
healthInfo["embedding_cache_misses"] = String(cacheStats.cacheMisses)
healthInfo["embedding_cache_hit_ratio"] = String(format: "%.2f", cacheStats.hitRatio)
```

Do NOT restructure the existing health response format - append new fields only.
  </action>
  <verify>
Run `swift build` - should compile without errors.
Grep for "getCacheStatistics" in EnhancedRequestHandler.swift to confirm wiring.
  </verify>
  <done>
Health endpoint includes embedding_cache_entries, embedding_cache_hits, embedding_cache_misses, and embedding_cache_hit_ratio fields when RAG is enabled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 1 VERIFICATION.md</name>
  <files>.planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md</files>
  <action>
Create VERIFICATION.md for Phase 1 following the format from Phase 4's verification document.

Use the SUMMARY.md as reference for what was built. Verify against the actual code in EmbeddingStore.swift.

Key truths to verify (from ROADMAP.md acceptance criteria):
1. Second identical query returns embedding from cache (no Ollama call)
2. Cache evicts oldest entries when limit reached
3. Metrics available: hits, misses, hit ratio
4. Thread-safe implementation via actor isolation

Required artifacts to verify:
- Sources/AIShellCore/RAG/EmbeddingStore.swift

Key links to verify:
- search() -> getCachedEmbedding() -> cache lookup
- search() -> setCachedEmbedding() -> cache insertion
- setCachedEmbedding() -> removeFirst() for LRU eviction
- getCacheStatistics() returns EmbeddingCacheStatistics

Requirements coverage (from ROADMAP.md):
- R1.1: LRU cache with configurable max size (default 1000)
- R1.2: Cache key = SHA256 hash of query text
- R1.3: Track cache hit/miss metrics for observability
- R1.4: Thread-safe implementation via actor isolation

Include line number references to actual code in EmbeddingStore.swift.
  </action>
  <verify>
File exists at .planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md
File has YAML frontmatter with phase, verified date, status, score
File has Observable Truths table with all 4 truths marked VERIFIED with line references
File has Requirements Coverage section with R1.1-R1.4
  </verify>
  <done>
Phase 1 VERIFICATION.md exists with complete truth verification, artifact verification, key link verification, and requirements coverage mapping to EmbeddingStore.swift implementation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 2 VERIFICATION.md</name>
  <files>.planning/phases/02-incremental-pruning/02-01-VERIFICATION.md</files>
  <action>
Create VERIFICATION.md for Phase 2 following the format from Phase 4's verification document.

Use the SUMMARY.md as reference for what was built. Verify against the actual code in EmbeddingStore.swift and MemoryStore.swift.

Key truths to verify (from ROADMAP.md acceptance criteria):
1. addDocument() uses heap-based pruning
2. addMemory() uses heap-based pruning
3. Pruning behavior unchanged (still keeps most recent/important)

Required artifacts to verify:
- Sources/AIShellCore/RAG/EmbeddingStore.swift (pruneDocuments method)
- Sources/AIShellCore/Memory/MemoryStore.swift (pruneMemories method)

Key links to verify:
- addDocument() -> pruneDocuments() with Heap<ScoredDocument>
- addMemory() -> pruneMemories() with Heap<ScoredMemory>
- ScoredDocument Comparable implementation (importance 0.6, recency 0.4 weights)
- ScoredMemory Comparable implementation (importance 0.7, recency 0.3 weights)

Requirements coverage (from ROADMAP.md):
- R2.1: Replace array sort in addDocument() with heap-based pruning
- R2.2: Replace array sort in addMemory() with heap-based pruning
- R2.3: Use swift-collections Heap for standard top-k selection pattern

Include line number references to actual code in both files.
  </action>
  <verify>
File exists at .planning/phases/02-incremental-pruning/02-01-VERIFICATION.md
File has YAML frontmatter with phase, verified date, status, score
File has Observable Truths table with all 3 truths marked VERIFIED with line references
File has Requirements Coverage section with R2.1-R2.3
  </verify>
  <done>
Phase 2 VERIFICATION.md exists with complete truth verification, artifact verification, key link verification, and requirements coverage mapping to EmbeddingStore.swift and MemoryStore.swift implementations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All M1 cleanup tasks: health endpoint with embedding cache stats, Phase 1 verification doc, Phase 2 verification doc.
  </what-built>
  <how-to-verify>
1. **Verify health endpoint wiring:**
   - Start the daemon: `./build.sh && ./build/AIShellDaemon`
   - In another terminal, test health: `curl http://localhost:8080/health` (or whatever port)
   - Confirm response includes: embedding_cache_entries, embedding_cache_hits, embedding_cache_misses, embedding_cache_hit_ratio

2. **Semantic matching smoke test (R5.4):**
   - Run a command like "list files" via the shell integration
   - Run a semantically similar command like "show files" or "display files"
   - Check daemon logs for "Semantic cache hit" or similar (should show cache hit on similar query)
   - Run a dissimilar command like "delete everything" - should NOT hit cache

3. **Review verification documents:**
   - Check .planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md exists and has correct format
   - Check .planning/phases/02-incremental-pruning/02-01-VERIFICATION.md exists and has correct format
  </how-to-verify>
  <resume-signal>Type "approved" if all three verifications pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Build verification:
```bash
swift build
```
Must compile without errors.

Code verification:
```bash
grep -n "getCacheStatistics" Sources/AIShellCore/Server/EnhancedRequestHandler.swift
```
Should show the new wiring in handleHealth().

Documentation verification:
```bash
ls -la .planning/phases/01-query-embedding-cache/01-01-VERIFICATION.md
ls -la .planning/phases/02-incremental-pruning/02-01-VERIFICATION.md
```
Both files should exist.
</verification>

<success_criteria>
1. `swift build` compiles successfully
2. Health endpoint returns embedding cache statistics when RAG is enabled
3. Phase 1 VERIFICATION.md exists with complete truth verification
4. Phase 2 VERIFICATION.md exists with complete truth verification
5. Human confirms semantic matching works as expected
</success_criteria>

<output>
After completion, create `.planning/phases/05-m1-cleanup/05-01-SUMMARY.md` documenting:
- Health endpoint changes with new fields added
- Verification documents created with key truths verified
- Human verification results for semantic matching
</output>
